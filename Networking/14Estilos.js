import { StyleSheet } from "react-native";

const estilos = StyleSheet.create({
    container: {
        flex:1,
        alignItems: "center",
        marginTop: 30
    },
    header: {
        fontSize: 30,
        fontWeight:"bold"
    },
    description: {
        fontSize: 20,
        paddingLeft: 10,
        textAlign:"center"
    }
})

export default estilos